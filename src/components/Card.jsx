import React from "react";

const Card = ({ fruit }) => {
  return (
    <div>
      <p>{fruit.name}</p>
      <p>{fruit.price}</p>
      <p>{price.description}</p>
    </div>
  );
};

export default Card;
